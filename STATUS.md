# Status

_Session-start document. Read this first — see the implementation brief (`discovery-phase1-implementation-brief-v2.2.md`) for full context._

## Done

**G1.4 — Scoring** ✅ closed 2026-09-16

- **New `scores` table** (migration `99655fdbe283`, same migration extends `filings` with `mission_text`/`program_text`/`significant_change_ind` and `organizations` with `website`) per §3, with one deliberate scope cut: `gap_rank` is left `NULL` — §4 lists "compute gap_rank ordering" under **Stage 6 (Publish)**, which is G1.5, not G1.4, so no ranking formula was invented here.
- **The 3-of-7 alignment framework wasn't in the implementation brief** — it lives in Lauren's original ICP spec and was never carried forward into brief text. Duan provided the 7 criteria directly this gate. Criterion 2 ("potential GENESIS hiring partner") is **excluded from scoring** per Duan's explicit instruction: GENESIS is a Track A–specific ARCHITECT service, Discovery is confirmed Track B–only, and there's no reliable public evidence to score it against without risking fabricated rationale. **Unconfirmed with Lauren — flagged for the Wednesday Sept 16 scoping call.** Threshold implemented as 3-of-6 over the remaining criteria (1, 3–7). Criterion 1 (priority-tier metro) is computed deterministically in Python (geography match against `icp_configs.config.geography_tiers`), not scored by the model.
- **Mission/program text scope was narrowed**: §4 called for "990 + website title/description" as Haiku's input, but there's no website-fetching mechanism and Stage 2 (G1.3) only extracted financials. Per Duan's explicit decision: extended Stage 2's XML parser (`extract_signals.py`) to also pull 990 Part III `MissionDesc`/`ActivityOrMissionDesc` and the numbered `ProgSrvcAccomActy{n}Grp` program descriptions (each independently citable, with $ amounts) — this is now the **sole** text source for Stage 3 scoring, no website fetching. Also extracts `WebsiteAddressTxt` (Item 5 disclosure, confirmed against real filings, placeholder values like "NONE"/"N/A" filtered) into `organizations.website` as a **citable dashboard link only** — never fetched, never a scoring input. Re-ran Stage 2 on 180 real survivors to backfill these new fields: 224/359 filings parsed (`coverage_pct` 0.624 — lower than G1.3's small-sample 0.867, expected variance from a larger, more varied sample), 154 orgs scoreable (147 with actual mission text).
- **Stage 3** (`stages/score.py` + `stages/run_scoring.py`): Haiku pre-screen (`claude-haiku-4-5`) + Sonnet deep pass (`claude-sonnet-5`) over the **Message Batches API** (§2, 50% discount), both using `output_config.format` (structured JSON outputs) rather than free-text parsing. `capacity` (the 2 public criteria, §4 Stage 2) and `soft_flags` (heavy-govt-funding, §4 Stage 2's ≥40% threshold, now finally consumed from `icp_configs.config.govt_funding_heavy_pct`) are **always computed in Python from `signals`**, never by the model.
- **§9 hard rules enforced as code, not just prompting**: `discovery.stages.score.enforce_hard_rules()` runs on every Sonnet response before persistence — forces `leadership_composition` to `score=null, needs_human_verification=true` if the model returned a score with no citation (rule 1: never infer race/ethnicity/gender from names), and redacts any rationale text containing "fully qualified" (rule 2). `capacity.note` is a fixed Python string, so the model has no path to write "fully qualified" there at all. 17 new unit tests exercise this directly (`tests/stages/test_score.py`), organized by which §9 rule they cover. `build_org_context()` never includes `ntee` (rule 6) — tested.
- **Hit a real API constraint live**: `output_config.format`'s JSON schema rejects `minimum`/`maximum` on integer properties (confirmed via a live 400 on the first real batch attempt — all 154 Haiku requests errored). Fixed by dropping those constraints and enforcing the 0–100 range via the system prompt instead; also improved `run_batch()`'s error logging to surface the actual `result.result.error` detail instead of just the result type, so this kind of failure is diagnosable without re-deriving it from scratch next time.
- **Ran the real pilot batch** against all 154 scoreable ARCHITECT survivors (brief's own "100-org pilot batch reviewed by Duan" acceptance criterion, with margin): 154/154 Haiku succeeded, 154/154 Sonnet succeeded, **0 hard-rule violations** needed sanitizing — the model followed the rules on its own, and the code-level enforcement is confirmed dormant-but-armed rather than doing silent cleanup work. Spot-checked results by hand: `leadership_composition` was `null`/`needs_human_verification=true` for **all 154** orgs (expected and correct — no website data, and 990 text rarely contains explicit self-description of leadership demographics; this is the designed common case, not a gap). Every non-null signal/criterion carried a citation grounded in the actual provided text or a specific financial figure. 2 orgs qualified (3+/6 alignment criteria): Midcoast Maine Community Action (community action agency, low-income/child-welfare focus) and Abbe Museum (Wabanaki Nations heritage museum, mid-capital-renovation) — both sensible, well-evidenced matches for ARCHITECT's stated ICP. 0 disqualifications (correctly conservative — none of these orgs stated grant-writing as their primary need).
- Anthropic API key landed in Key Vault as **`appadino-discoveryAI-key`**, not `anthropic-api-key` — code and docs reference it as-is rather than requiring a rename. Wired into the deployed Container Apps Job the same way `DATABASE_URL` is (Key Vault-sourced Container Apps secret, `ANTHROPIC_API_KEY` env var) so `discovery.cli score` is runnable via the deployed job, not just locally. Also hit (and worked around, not fixed) a real account-level blocker: the key initially had zero credit balance — resolved by Duan adding credits mid-gate, confirmed live before the real run.
- `discovery.cli score <client_id> [--haiku-cut-n N]` runs Stage 3 and logs one `runs` row (`stage='scoring'`) with `scoreable`, `haiku_succeeded/failed`, `sonnet_candidates`, `sonnet_succeeded/failed`, `hard_rule_violations`.
- Suite is now 121/121 (104 in `pipeline/tests/` + 17 new in `test_score.py`), ruff/mypy clean. Rebuilt and pushed `discovery-pipeline:latest` to ACR twice this gate (once for the extraction changes, once after the schema-validation fix) so the deployed job has this gate's code.
- Fixed one test in `test_g13_comprehensive.py` (a G1.3 review file from the test-developer role, written concurrently with this gate) whose exact-key-set assertion on `parse_990_xml`'s output broke against this gate's legitimate new fields — narrowed to `issubset` since the test's actual intent (NTEE never appears) still holds regardless of how many other fields the function grows to include.

## Next

**G1.5 — Suppress + triggers + publish**: Stage 4 (suppression lists — ARCHITECT's seed data isn't loaded yet), Stage 5 (filing-derived triggers), Stage 6 (publish: `prospects` table, **`gap_rank` ordering computed here**, CSV export, QA job, alert rules). This is also the gate whose acceptance criterion is "end-to-end national run completes unattended" — worth deciding early whether that means a true full-national run (119,218 survivors × Stage 2/3, which at the per-org rates observed this gate and last would take a very long time — see the Stage 2 performance tradeoff noted in G1.3) or a bounded/representative run, before committing to an approach.

## Open questions / decisions awaiting Duan

- **Flagged for the Wednesday Sept 16 scoping call** (per the brief's own checkpoint): the GENESIS criterion exclusion from alignment scoring (see above) needs Lauren's sign-off — go with 3-of-6 permanently, add GENESIS back in once real evidence sourcing exists, or handle it some other way.
- Not blocking, worth deciding before G1.5: what "end-to-end national run completes unattended" should actually mean at G1.5's acceptance bar, given Stage 2/3's demonstrated per-org cost at pilot scale.
- Carried over from G1.3, still not blocking: the Stage 2 national-scale performance tradeoff (per-filing remote-zip reads vs. bulk zip downloads).

## Notes for cold-start

- Azure CLI is signed into the Pay-As-You-Go subscription used for this project; `rg-appadino-discovery-dev` is the only resource group so far.
- The Postgres admin password is stored as the `postgres-admin-password` secret in `adisc-dev-kv` (alongside `db-connection-string`, which already has it baked into the connection URI).
- Postgres firewall has an `AllowDevMachine` rule for this dev machine's IP (added during G1.2 to run migrations/ingest locally) — IP-specific, low risk, but flagging it exists in case it needs rotating or removing later.
- The Anthropic API key is in Key Vault as **`appadino-discoveryAI-key`** (not `anthropic-api-key`) — has a real credit balance as of this gate.
- No enrichment code exists anywhere, per §5.5/§9 — correct until gate E1 is explicitly opened.
- `discovery.clients.propublica.get_organization()` still exists and is still unused — hasn't become relevant yet; opportunistic backfill of survivors with missing/stale BMF fields is a real future use, not urgent.
- The ARCHITECT client's id in the dev DB is **2**, not 1 — a downgrade/re-upgrade cycle during G1.3 while fixing a JSON double-encoding bug in the seed migration consumed id 1. Harmless, but don't assume `client_id=1` when testing.
- 154 real `scores` rows (both `haiku` and `sonnet` stage) now exist in the dev DB for `client_id=2` from this gate's pilot run — real data, not test fixtures; fine to leave in place or clear before a future real run.
