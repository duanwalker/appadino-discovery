# Status

_Session-start document. Read this first — see the implementation brief (`discovery-phase1-implementation-brief-v2.3.md`) for full context._

## Done

**G1.5 — Suppress + triggers + publish** ✅ closed 2026-09-16, with one acceptance criterion deliberately left open (see below)

- **New tables** (migration `08e29df9a3e2`): `suppression`, `prospects`, `qa_samples`, plus deliberate deviations from the brief's §3 sketch, all documented in the models themselves: `suppression.partner_window_expires_at` (the 18-month attribution window §4 mentions but doesn't schema); `prospects.trigger_angle`/`trigger_evidence` (angle resolved from config, evidence citable, alongside the brief's `assigned_trigger`); `qa_samples.citation` instead of `citation_url` (our citations are inline text/pointers, not URLs — "re-fetch" means re-querying our own stored source data, not an HTTP fetch).
- **Stage 4** (`stages/suppress.py`): loaded ARCHITECT's real seed suppression list via migration `7ac7a7b1f96a` — 12 past/current clients + 5 active prospects (§4 Stage 4), all name-only, no EINs given. EIN-exact matches suppress outright; fuzzy name matches (difflib, threshold 0.85, names normalized — legal-suffix words and punctuation stripped before scoring, see post-close fix below) are flagged in `prospects.notes`, never silently dropped — §9 rule 7 as executable code, not just a principle. Verified via unit tests using the actual seed names (e.g. "Butterfly Dreamz"); **not** demonstrated firing in a live run, since the real pilot data (Maine nonprofits) has no genuine overlap with ARCHITECT's Cincinnati/Charlotte-based list — flagged honestly rather than claimed as end-to-end proven.
- **Stage 5** (`stages/triggers.py`): all 4 filing-derived triggers from §4 (new ED/CEO, DD departure, transformational revenue jump ≥50%, first filing above the client's revenue floor), each diffing an org's latest vs. prior filing. An org can fire more than one; a documented priority order picks the "assigned" one but every detected trigger is kept in `trigger_evidence`. **The brief's trigger→angle table has a real gap**: only 3 of the 4 triggers have a default angle ("first filing above floor" isn't covered). Per Duan's explicit instruction, `trigger_angle` stays `null` for that trigger rather than inventing text — Lauren's own rule ("the trigger shapes the message") makes a fabricated angle actively risky, not just untidy.
- **Stage 6** (`stages/publish.py` + `stages/run_publish.py`): upserts `prospects` for every Sonnet-scored, non-disqualified, non-EIN-suppressed survivor; **`gap_rank` finally computed** (was left `NULL` in G1.4 — §4 puts it under Publish, not Scoring). No formula is specified anywhere in the brief beyond "rank on gap, composition over size" — implemented as a transparent, config-weighted composite (`alignment=0.5, capacity_gap=0.3, trigger=0.2` defaults, tunable via `icp_configs.config.signal_weights`) and clearly flagged as first-pass, not authoritative. **Worth knowing**: with the default weights, a 2-of-6-aligned org *with* a trigger (gap_rank 66.67) currently outranks a 3-of-6-qualifying org *without* one (gap_rank 55.0) — mathematically consistent with the stated weights, but a real, non-obvious consequence worth ARCHITECT's eyes before treating the ranking as authoritative. CSV export goes to `pipeline/output/` (gitignored) — no Azure Blob Storage is provisioned yet, so "CSV out to blob" (§2) is a local file for now, flagged as a real gap, not silently substituted.
- **QA job** (`stages/qa.py`, §7): samples up to 20 cited claims per run, verifies each against our own stored source data (numeric fields with tolerance, boolean/categorical fields, and — for actual quoted/paraphrased text, which is what Sonnet produces in practice, not just bare field-name pointers — a longest-common-substring check against the real `mission_text`/`program_text`). **Three real bugs were found and fixed by actually running this against G1.4's live scored data**, not just unit tests: (1) `revenue_composition`'s nested fields (`govt_pct`/`program_pct`/`contributions_pct`) weren't flattened into the QA context, so every numeric citation false-mismatched — first observed as a 100% mismatch rate; (2) the text verifier only recognized bare field-name pointers, not real quotes, so genuine citations fell through as "unverifiable"; (3) `revenue_trend`/`significant_change_ind` citations weren't recognized as fields at all (`significant_change_ind` isn't even part of `compute_signals()`'s output — it lives on `filings`, not `signals` — a real cross-table gap). Mismatch rate went 100% → 25% → 10% → **0%** across four real runs as each bug was found and fixed; confirmed stable (0%) across two independent random samples once fixed. This is a genuine validation of G1.4's scoring data, not a rubber stamp — it would have caught real hallucination if any existed.
- **Alerts** (§7, explicitly in G1.5's scope line): a real Bicep addition — `adisc-dev-ops-ag` action group (email) + 3 `Microsoft.Insights/scheduledQueryRules` watching `ContainerAppConsoleLogs_CL` (confirmed against real pipeline logs, not guessed) for `RUN_FAILED`, `ZERO_OUTPUT_RUN`, and `QA_MISMATCH_RATE_EXCEEDED` markers, which all four pipeline orchestrators now log explicitly. §7's third category, **cost anomalies, is not implemented** — no historical cost baseline exists yet to detect an anomaly against. Small new recurring cost (~3 scheduled query rules, roughly $1.50–2/mo each).
- **Ran the real (bounded) pipeline** against G1.4's 154 scored ARCHITECT survivors, per Duan's explicit direction to validate on a real bounded sample rather than attempt the full 119,218-survivor national run blind this gate: 154 published, 0 EIN-suppressed, 0 fuzzy-flagged (see Stage 4 caveat above), triggers detected on 97/154 (19 new_ed, 7 transformational_revenue_jump, 71 first_filing_above_floor), QA mismatch rate 0% (post-fix), CSV and `prospects`/`scores.gap_rank` all verified populated correctly.
- **G1.5's own acceptance criterion — "end-to-end national run completes unattended" — is deliberately NOT met this gate.** Per Duan's explicit direction, the full 119,218-survivor run was not attempted: Stage 2's per-filing remote-zip-read performance (flagged in G1.3, carried forward through G1.4) makes a true national run impractical as currently built — likely many hours to days, not something that "completes unattended" in any reasonable sense. The Stage 2 performance fix (bulk-download each of the ~40 distinct monthly ZIP archives once instead of one remote read per filing) was also not attempted blind this gate. This is an honest open item, not a claimed-but-unproven acceptance.
- `discovery.cli publish <client_id>` runs Stages 4-6 + QA and logs one `runs` row (`stage='publish'`).
- 52 new unit tests across `test_suppress.py`, `test_triggers.py`, `test_publish.py`, `test_qa.py` (the latter grew during live debugging as real bugs surfaced — 31 total). Suite: 212/212 passing, ruff/mypy clean. Rebuilt and pushed `discovery-pipeline:latest` to ACR (structured alert-logging + all QA fixes).
- **Post-close fixes (2026-09-18), from an independent Copilot test review** (`TEST-COVERAGE-GAPS.md` "G1.5" section): (1) `normalize_name()` now strips legal-suffix words (Inc./LLC/Corp./Corporation/Foundation/Incorporated) and punctuation before fuzzy-scoring — fixes two real near-misses Copilot found (e.g. "Butterfly Dreamz, Inc." scored 0.842 against seed "Butterfly Dreamz", just under the 0.85 threshold; now scores 1.0). A full audit of the live 154-org `client_id=2` dataset against all 17 seed names, before and after the fix, found **0 real suppression misses** in the current data — the Maine pilot has no genuine name overlap with ARCHITECT's real client list — so publish counts are unchanged (154 published, 0 EIN-suppressed, 0 fuzzy-flagged) and no re-run was needed. (2) `compute_gap_rank()` now clamps `criteria_met_count` to `[0, 6]` and normalizes a custom `signal_weights` config proportionally when its sum exceeds 1.0, with a `[0, 100]` output backstop — closes a real "no defensive clamp" gap Copilot found (a mistuned config could previously produce values like 300.0). Suite: 258/258 passing, ruff/mypy clean. Rebuilt and pushed `discovery-pipeline:latest` to ACR.
- **G1.4 post-close fixes (2026-09-18), from a separate independent Copilot test review** (`TEST-COVERAGE-GAPS.md` "G1.4" section, `test_g14_llm_guard_gaps.py`): four of six flagged gaps fixed, in priority order. (1) **Demographic-inference guard (hard rule 1)** — `enforce_hard_rules()` now verifies a `leadership_composition` citation is an actual published self-description quoted from the org's own `mission_text`/`program_text` (same LCS technique as the QA job, but bypassing `verify_claim()`'s field-presence-only shortcut, which would have left the exact same loophole open) — a citation naming a real field but not containing a real self-description (e.g. attached to a name-based demographic inference) is now rejected. (2) **Citation enforcement extended beyond `leadership_composition` (hard rule 4)** to the other four values signals and every alignment criterion — a null/empty/malformed/hallucinated citation now nulls the score or forces `met=false` instead of passing through. (3) **`dq_reason` "fully qualified" redaction gap fixed** via a new `redact_dq_reason()`. (4) **Batch partial-failure durability**: a failed batch item now gets a real placeholder `scores` row (`values_signals`/`alignment` NULL) instead of no row — durable and queryable per-EIN, excluded from `load_publishable()` via a new `alignment IS NOT NULL` clause, recoverable via the existing upsert on re-score; verified against the live dev DB with a synthetic EIN. (5) **Haiku top-N tie-break made deterministic** — explicit `(-pre_score, ein)` secondary sort key. **Left open, by design**: the sole-primary-need-vs-one-of-several-needs boundary (a genuine semantic judgment, not mechanically validatable without another LLM pass) remains prompt-only — documented as a deliberate boundary, not a gap. Suite: 324/324 passing, ruff/mypy clean. Rebuilt and pushed `discovery-pipeline:latest` to ACR.
- **Same-day follow-up fixes (2026-09-18) to the G1.4 post-close fixes above** — Duan caught two real problems in the first pass's own design (`TEST-COVERAGE-GAPS.md` "G1.4" → "Follow-up fixes" section): (1) `_citation_supports_claim()` (the Gap 3 fix, covering the four non-leadership signals + alignment criteria) called `qa.verify_claim()` directly, which carries the exact field-presence-only shortcut the Gap 1 leadership fix was specifically written to avoid — a fabricated `"mission_text: '...'"` citation on e.g. `population_served` would have passed as grounded merely because the org has *some* mission text. Fixed by branching on citation shape: numeric/boolean/categorical field pointers still defer to `verify_claim()` (already value-accurate, no loophole), but text-field pointers and free-text citations now check directly against the real corpus, matching leadership's treatment. (2) `enforce_hard_rules()`'s `org_context` param, which defaulted to `None` and silently skipped all grounding checks when omitted, was a real silent-bypass risk for the highest-priority guard in the review — any future caller that forgot to pass it wouldn't error, it would just quietly re-open the loophole. Made required (no default); omitting it is now a `TypeError`, not a silent downgrade. `run_scoring.run_scoring()` (the only production caller) needed no changes. ~26 test call sites across `test_score.py`/`test_g14_comprehensive.py` were updated to pass a real, genuinely-grounded `org_context` fixture instead of relying on the removed fallback. Suite: still 324/324 passing, ruff/mypy clean. Rebuilt and pushed `discovery-pipeline:latest` to ACR.
- **G1.3 post-close fixes (2026-09-18), from a third independent Copilot test review** (`TEST-COVERAGE-GAPS.md` "G1.3" section, `test_g13_filter_signal_gaps.py`): three lower-severity fixes, batched after the G1.4 items. (1) **`coverage_pct` denominator** — `extract_signals_for_survivors()` now divides `signals_computed` by Stage 1's survivor count (`len(eins)`), not `filings_parsed / (filings_parsed + filings_failed)` — a survivor with zero filing rows at all was previously invisible to the old denominator, so the metric could read as complete coverage even when real survivors were never parsed. (2) **Development-role abbreviation detection** — a new word-boundary check catches `"dev"` (e.g. "VP Dev") alongside the existing full-word keywords, without loosening into a bare substring match that would false-positive on "IT Developer"/"Device Manager"; adjacent-role synonyms like "Donor Relations Lead" remain correctly out of scope, left open by design. (3) **Government-funding threshold rounding** — `gov_funding_pct` (the value `compute_soft_flags()`'s `>=40%` comparison actually uses) is no longer rounded before that comparison; the display/citation copy in `revenue_composition["govt_pct"]` still rounds, since it's never itself compared against a threshold. The threshold decision stays at Stage 3 (couldn't move to Stage 2, since `govt_funding_heavy_pct` is per-client config and the shared `signals` table has no client context) — the fix is to stop pre-rounding the raw value that decision operates on. Suite: 325/325 passing, ruff/mypy clean. Rebuilt and pushed `discovery-pipeline:latest` to ACR.

**E1 — FullEnrich validation spike** ✅ closed 2026-09-19 — **GO**, with a caveat that changes E2's UI scope (see below)

- Standalone script (`pipeline/scripts/e1_fullenrich_spike.py`), run against real `client_id=2` data per the brief's §5.5/§8 acceptance criteria: no pipeline integration, never auto-triggered. New `enrichments` table pulled forward via migration `b7d4f1a9c3e6` to receive real results (brief's §3 schema, applied as-is).
- **Raw numbers**: 20 `approved` prospects, 19 submitted (1 skipped — no website/domain on file), 17 raw matches (89% match rate — clears the brief's ≥60% bar), 12/15 emails FullEnrich-verified (80% — clears the ≥70% bar), 135 credits spent (11.2 credits/verified contact, comfortably inside the $500/12,500-credit pack). Full numbers: `pipeline/reports/e1_spike_20260919.md` + `_contacts.csv`.
- **Manual spot-check (Duan, all matches, not just the 10-item sample) found this raw match rate overstates usable contacts**: only **11 of 17** are genuinely current, correct contacts — the rest have stale-employer or domain-mismatch problems (e.g. a contact's email resolved to a *different, current* employer's domain, not the org being prospected; FullEnrich's own "verified" SMTP status doesn't catch this, since the address is real and deliverable — it's just the wrong person's current job). **This is being treated as an expected, routine property of enriching contacts sourced from 990 officer lists, not a one-off fluke**: officers named on a filing are frequently 12-18 months stale by the time they're enriched (filing lag, §10 risk already named for a different reason), and unlike a CRM-sourced contact, there's no independent "still employed here" signal feeding the match. Expect this on every future batch, not just this one.
- **Decision: GO.** Match rate and FullEnrich's own verified-rate both clear the brief's adoption threshold on the metrics gate E1 was actually scoped to test (match rate, verified-email rate, cost per usable contact, API ergonomics). The spot-check finding doesn't change that verdict — FullEnrich is doing its job (finding real, deliverable, verified addresses) — but it does mean **"verified" must not be presented to users as "confirmed current."** Carried into E2: the contact-columns design (brief §5, v2.3) needs a distinct signal for human-confirmed-current vs. provider-verified-deliverable — not yet designed, flagged here so E2 doesn't inherit the gap silently. Does not by itself unblock delivering enrichment results to ARCHITECT — the separate FullEnrich reseller-terms reconciliation (below) still gates that.
- **Two real gaps found and fixed post-run, before E2 can build on the same table**: (1) email-only matches (no phone) were writing `enrichments.provider_confidence = NULL` — violates hard rule 3's "always stored with provider + confidence + verification status + retention expiry." Fixed with a documented fallback (`EMAIL_STATUS_CONFIDENCE` heuristic map in the script, since FullEnrich's v2 API has no native numeric email confidence) and backfilled the 5 affected rows from their already-stored `raw` JSON, no re-enrichment/re-spend needed. (2) `retention_expires_at` was independently verified correct (`completed_at` + 90 days) on all 17 rows — no gap there.
- API ergonomics (§5.5 point 5): asynchronous (POST returns only an `enrichment_id`; polled `GET .../bulk/{id}`, no webhook receiver available to a CLI script). No `Sub-Account-Id` header appears anywhere in FullEnrich's public v2 docs, despite the brief's §5.5 design assuming one — `icp_configs.config.fullenrich_subaccount_id` is still unset for client_id=2; this run authenticated with the Bearer API key alone. Needs a real answer during the reseller-terms reconciliation, not just an assumption carried into E2.
- Found and fixed a real parsing bug the same session: FullEnrich's actual response nests contact fields under `contact_info`, not top-level as the public docs page's own summary implied — caught because the batch reported nonzero credits spent while the first parse showed 0 matches. Fixed and re-processed the same completed batch (`--replay-enrichment-id`, a free GET) rather than re-spending credits.

## In progress

**G2.x — Dashboard, built and now live-verified against real data — still awaiting Duan's browser review, not yet accepted.**

- **Groundwork fix (independent of the UI, done first per Duan's explicit direction)**: STATUS.md's own prior "Next" note assumed `prospects.notes` already supported what the dashboard needed — checked explicitly, and it didn't. `run_publish.py` unconditionally overwrote `notes` on every republish (`notes = f"Possible suppression match..." if ... else None`), which would have silently destroyed any human review comment on the next scheduled run (§7 weekly score refresh). Fixed via migration `4a2e9c1f7b3d`: new `prospects.suppression_flag` column takes the pipeline-generated fuzzy-suppression message; `notes` is now purely human-owned, and `upsert_prospect`'s `ON CONFLICT` no longer touches `notes` or `updated_by`. Also added a `ck_prospects_status` CHECK constraint scoped to the four states the dashboard actually uses (`new|reviewed|approved|rejected`) — the brief's §3 sketch's other two (`contacted|responded`) belong to the Phase 2 Outreach queue, out of scope here. No data migration needed (0 fuzzy-flagged prospects in the current 154-row dataset). Pipeline suite still 325/325 passing, ruff/mypy clean after the change.
- **Functions API** (`dashboard/functions_api/`, Python Azure Functions v2 model): now 10 routes — the original 8 (prospect list/update, suppression-flag confirm/dismiss, suppression CRUD, CSV export, run history) plus E2's two enrich routes (see below). Imports `discovery.models`/`discovery.db` directly from the pipeline package rather than redeclaring the schema (see its README.md for the import-strategy decision Duan asked to pin down: editable path install for local dev, wheel-vendoring at deploy time **not yet implemented** — flagged, not built). Write surface is deliberately narrow: only `prospects.status`/`notes`, `suppression` rows, and now `enrichments` (enrich routes only) are human/system-editable; everything the pipeline writes stays read-only here.
- **React dashboard** (`dashboard/web/`, Vite + TypeScript): prospect review table (rank, org, revenue, gap rank, criteria met, trigger, expandable citations for all 5 values signals + 6 alignment criteria + capacity), checkbox-fast status buttons, per-prospect notes editor, suppression-flag confirm/dismiss banner inline in the row, a separate suppression-manager tab (add/remove `suppression` entries), one-click CSV export (same column set as the existing `pipeline/output/prospects_client2.csv`, pending Duan's own check against Lauren's original POC file), and a run-health banner (last publish run's timestamp/status/published count/QA mismatch rate, sourced straight from `runs.counts` — no schema change needed there). Now also renders E2's five contact columns (see below). Client switcher intentionally not built — hidden behind a `CLIENT_ID` constant per §5, single-tenant for now.
- **What's verified, now against real data**: Duan supplied a real `dashboard/functions_api/local.settings.json` (gitignored, holds the real dev `DATABASE_URL`) this session — the previous gate's blocker is resolved. `func start` + live `curl` against `http://localhost:7071` confirmed the full `prospects` list, `export.csv`, and (new) `enrich`/`enrich-status` routes render/behave correctly against the real 154-row `client_id=2` dataset, not a placeholder connection string. Still not a browser click-through — this session verified over raw HTTP, not through the rendered React UI in an actual browser window. Full pipeline suite (371 tests) + Functions API suite (54 tests) + ruff/mypy clean; React app type-checks clean under strict TS and builds for production.
- **Not built this pass, flagged not silently dropped**: no deployment infra (`staticwebapp.bicep` / Functions app registration) — this only runs locally via `func start` so far. No auth on the Functions API — fine for a single-operator local review, not for a public Lauren-facing URL.
- **Local-run gotcha found this session**: `func start` silently falls back to the system Python (not `.venv`) and fails with `ModuleNotFoundError: No module named 'psycopg'` unless the venv is actually activated first (`source .venv/Scripts/activate` in bash, or the PowerShell equivalent) — prepending `.venv/Scripts` to `PATH` alone is **not** sufficient, Core Tools' worker selection appears to key off `VIRTUAL_ENV` rather than resolving `python` off `PATH`. Not a code bug, just a real local-dev friction point worth knowing before the next cold start.

**E2 — Enrichment stage, built and live-verified against real data — awaiting Duan's review (same propose-then-review pattern as every prior gate).**

- **Brief-alignment gap found before starting**: v2.3's own changelog only covers the 5-column contact redesign. It does **not** document two decisions STATUS.md had already flagged as carried into E2 (a "stale — likely moved" status, and a real-vs-verified confidence signal) — those were open items in STATUS.md, never actually written into the brief. This session's design decisions for both are recorded here; the brief itself should get a v2.4 changelog entry the next time it's touched, so it stops being a step behind what's actually shipped.
- **`enrichments` migration** (`c1e8a4f2d5b9`, applied to the dev DB): widened the `email_status` CHECK constraint to add `stale_likely_moved` (fourth bucket, alongside verified/catch_all/not_found), and added `stale_detail text` (the mismatched domain/company that triggered it, for the dashboard tooltip). No separate confidence column — see the adapter design below for why.
- **Provider interface + FullEnrich adapter**: `discovery.clients.enrichment_provider.EnrichmentProvider` (submit-returns-job-id, poll-checks-status — E1's confirmed async shape, not synchronous) and `discovery.clients.fullenrich_adapter.FullEnrichProvider`, the first real implementation.
- **Stale-detection / confidence signal — one mechanism, with a real false-positive caught before shipping**: the adapter compares the enriched email's domain against the org's own website domain. A mismatch (e.g. Day One's officer resolving to a sheppardpratt.org address, E1's original finding) sets `email_status = stale_likely_moved` regardless of FullEnrich's own reported status — this **is** the real-vs-verified confidence signal STATUS.md flagged as undesigned, folded into one mechanism rather than built as two. **Caught in review before implementation**: a naive domain-only check has a false-positive mode — unpaid nonprofit board officers very often list a personal address (gmail, yahoo, etc.) on the 990 even when their listed role is completely current, which a domain check would otherwise misread as "moved." Fixed with a `PERSONAL_EMAIL_DOMAINS` carve-out (not exhaustive by design — an unlisted personal provider just means an occasional wrongly-flagged stale, not a wrongly-cleared one, the safer direction per hard rule 3's "blank beats wrong").
- **Hard rule 8 as executable code**: `discovery.stages.enrich.can_enrich(status, enrichment_enabled, fullenrich_subaccount_id)` — pure function, one guard used by both enrich routes, tested for each of the three failure branches independently plus the success path. Verified live: a real POST to `/api/prospects/{approved_id}/enrich` against client_id=2 (whose `enrichment_enabled` is genuinely `false`, see below) returned `403 {"error": "enrichment is not enabled for this tenant"}` — the guard fires correctly against real tenant config, not just in unit tests.
- **Per-prospect Enrich button, sync-with-bounded-fallback, not blind synchronous polling**: `POST /prospects/{id}/enrich` submits and polls for a short bounded window (~12s worst case) before returning `202 {status: "pending", job_id}` instead of blocking further; `GET /prospects/{id}/enrich-status?job_id=` resumes. Chosen because E1 never actually measured real single-contact submit-to-completion latency (its report has match-rate/cost data, not wall-clock timing, and the timestamps sitting in the dev `enrichments` table turned out to be from the post-hoc bug-fix *replay* — a free GET on an already-finished job — not the original submission, so they don't answer the question either). Rather than guess a number and hope it's under whatever Azure Functions HTTP timeout tier ends up in play, the design just doesn't need the guarantee. The dashboard button shows a spinner immediately, not a silent hang, and polls (still spinner-visible) up to ~2 minutes client-side before telling the user to check back later. Persistence is idempotent on the provider's job id, so a double-click or a status-poll racing the initial submit can't double-write or double-count credits.
- **Real bug found and fixed against live data, not just synthetic tests**: the first version of `select_primary_officer()` reused `triggers.py`'s `EXECUTIVE_TITLE_KEYWORDS` ranking (built for a different purpose — detecting *when* the top exec changes, not picking the *best* contact). Run against Day One's real 2024 filing (EIN 010322532), it picked the unpaid Board President (Katie Grant) over the actual CEO (Cassandra Humphrey) because "CEO" isn't in that keyword list, and it would have picked a departed CEO (Gregory Bowers, "CEO (THRU MAR 2024)") over his own current successor in another ordering, since nothing deprioritized departed officers. Fixed with a dedicated, local priority list (paid-staff titles first, "president" last) and a departure-marker check that always ranks a current officer over a departed one regardless of title. Confirmed fixed live: Day One's Name/Title now correctly shows Cassandra Humphrey, CEO.
- **A second real thing the same live check surfaced**: Day One's dashboard row now shows Name/Title = Cassandra Humphrey (current, correct) but Email/Phone/Status = Gregory Bowers's old E1-spike enrichment data (correct too — that's genuinely who was enriched, and email/phone are never re-derived from a name match). Since Name/Title always tracks the current officer while an enrichment result stays pinned to whoever was actually submitted, these can legitimately diverge whenever leadership changes between an enrichment and a later filing update — exactly the open question the brief itself already flags in §5 v2.3 ("does a confirmed enrichment ever update Name/Title, or do they always stay as the original 990 entry"). Rather than silently pick an answer, added `contact.contact_mismatch` (plus `enriched_contact_name`/`enriched_contact_title`) so the dashboard visibly flags it — "⚠ enriched for Gregory Bowers, CEO (THRU MAR 2024)" under the email — instead of implying the enriched contact info belongs to whoever Name/Title currently shows. Confirmed firing correctly against Day One's real row.
- **Dashboard contact columns** (§5 v2.3): Name/Title always rendered, free, from `filings.officers` via the fixed selector above, labeled unverified, not gated by `enrichment_enabled`. Email/Phone/Status locked (`🔒 Enrich to unlock`, tooltip) until enriched; same literal text in the CSV export cell, not a blank cell. Retention-expired contacts are visually grayed with an "expired {date}" note, both table and CSV-adjacent. `stale_likely_moved` shows as its own badge ("Stale — likely moved") distinct from Not found/Verified/Catch-all, with the mismatched domain on hover.
- **ARCHITECT tenant flag (client_id=2) confirmed correct, no fix needed**: queried the live dev DB directly — `icp_configs.config.enrichment_enabled` is genuinely `false` for the active config row. No migration in git history ever set it `true`, so the suspected leftover E1-visibility toggle either never happened or was already reverted; either way, current state is correct and matches Lauren's decision.
- **What's verified**: pipeline suite 371/371, Functions API suite 54/54 (13 new, covering the guard's three failure branches, the done/pending/idempotent-persist paths on both routes, and the contact-mismatch signal), ruff/mypy clean across both; React app type-checks and builds clean. Live-verified end-to-end against the real dev DB: contact payload shape, the officer-selection fix, the contact-mismatch signal, the hard-rule-8 403 on a real approved prospect, and the CSV export's contact columns.
- **What's NOT verified — genuine gap**: no real FullEnrich API call was made this session (no `FULLENRICH_API_KEY` available here, and `enrichment_enabled` is correctly `false` for the only tenant anyway, so a real Enrich click would be blocked even with a key). The `FullEnrichProvider.submit`/`poll` methods are unit-tested against a faked `discovery.clients.fullenrich` module, not a live FullEnrich response. No deploy infra, same as G2.x. No browser click-through of the Enrich button's spinner/pending UI — verified by code review + the underlying route behavior, not by watching it render.
- **Per-tier enrichment credit cap explicitly deferred to a future E3**, per Duan's direction this session — out of E2's scope entirely, not just unbuilt.
- **§8's E2 acceptance line "sub-account ID passed on every call" is not literally met, inherited from E1's already-documented gap**: `FullEnrichProvider` stores `subaccount_id` and is wired to use it the moment there's a real header to send, but no `Sub-Account-Id` (or equivalent) header exists anywhere in FullEnrich's public v2 docs (confirmed by E1) — sending nothing is the only honest option until the reseller-terms reconciliation (open item above) resolves this. Not a new gap, just carried forward and worth naming against the brief's literal acceptance wording rather than assuming it's silently satisfied.

## Next

**Before G1.5 can be considered fully accepted**: the Stage 2 performance fix (bulk zip download) and an actual full-national unattended run. This isn't a new gate in the brief's numbering — it's finishing what G1.5 deliberately deferred. Worth deciding whether to tackle it as its own session or fold it into whatever's next.

**G2.x + E2 — Dashboard + Enrichment**: both are built and live-verified against real data but still need Duan's actual browser review (see "In progress" above). Once both are accepted: deploy infra (Static Web Apps + Functions app registration, wheel-vendoring the `discovery` package into the Functions deploy payload), Duan's own check of the CSV export against Lauren's original POC file, a real `FULLENRICH_API_KEY` in `local.settings.json` to exercise one live Enrich click end-to-end, and the brief's own v2.4 changelog entry for the stale-status/confidence decisions this gate made.

## Open questions / decisions awaiting Duan

- **Still flagged for Lauren, carried from G1.4**: the GENESIS criterion exclusion from alignment scoring.
- **New this gate**: the default `gap_rank` weights produce the counterintuitive ranking described above (unqualified-with-trigger above qualified-without-trigger) — worth ARCHITECT's input on whether that's actually the right prioritization before treating any CSV export as ready to hand to Lauren.
- **New this gate**: when (or whether) to invest in the Stage 2 performance fix + a genuine full-national run — this is the one piece of G1.5 that's still open, not a new ask.
- Not blocking: no Azure Blob Storage is provisioned for CSV export (§2 says "CSV out to blob") — currently a local file under `pipeline/output/`. Fine for continued pipeline development; would need real infra before a dashboard or external handoff depends on it.
- Not blocking: cost-anomaly alerting (§7's third category) has no implementation — no historical baseline to detect an anomaly against yet.
- **FullEnrich reseller-terms reconciliation needed** (§5.5). §5.5 documents reseller terms as "confirmed by Hugo on Sept 15, 2026." A Sept 18 follow-up call added detail: Hugo gave 5,042 free trial credits (valid 3 months), said the reseller program must be purchased to continue after that, and said the current $500/12,500-credit pack requires signing a contract — consistent with a real (if entry-tier) reseller path, not a bare self-serve account. However, reseller pricing tiers quoted this call (100k credits/$3k, or 750k/$2,250) don't obviously reconcile with the ~$4,000 reseller figure from an earlier conversation. Get one written line from Hugo confirming: does the $500/12,500-credit pack's contract constitute the Reseller Agreement referenced in §5.5, and how does it relate to the $4,000 figure? **Still unresolved even though E1 itself closed GO (see "Done" above)** — E1's go/no-go was scoped to the adoption-threshold metrics only; treat E1's spike data as internal validation only, not yet cleared for delivering results to ARCHITECT, until this is confirmed.
- **New this gate: does the contact-mismatch UI (Name/Title vs. the enriched contact) go far enough?** Right now it's a visible warning note, not a block on anything — worth Duan's read on whether that's sufficient or whether a diverged prospect should also prompt a re-enrich suggestion.
- **New this gate: is the `PERSONAL_EMAIL_DOMAINS` carve-out list (gmail/yahoo/hotmail/outlook/icloud/aol/...) good enough as a fixed, unmaintained list**, or does it need to live in `icp_configs.config` as tunable data (consistent with "config is data, not code," §9) if it turns out to need frequent additions in practice.

## Notes for cold-start

- Azure CLI is signed into the Pay-As-You-Go subscription used for this project; `rg-appadino-discovery-dev` is the only resource group so far.
- The Postgres admin password is stored as the `postgres-admin-password` secret in `adisc-dev-kv` (alongside `db-connection-string`, which already has it baked into the connection URI).
- Postgres firewall has an `AllowDevMachine` rule for this dev machine's IP (added during G1.2 to run migrations/ingest locally) — IP-specific, low risk, but flagging it exists in case it needs rotating or removing later.
- **Anthropic API key rotation is now complete end-to-end (confirmed 2026-09-18).** New naming convention: the Key Vault secret is named with a month suffix — currently **`appadino-discovery-2026-09`** in `adisc-dev-kv` — rather than the old fixed `appadino-discoveryAI-key`/`anthropic-api-key` names; expect to rotate again (new secret, new month suffix) on the next rotation rather than overwriting this one in place. Verified: (1) the secret exists, is enabled, and holds a real 108-char `sk-ant-...` value; (2) the Container App Job (`adisc-dev-pipeline-job`) was found still pointing its `anthropic-api-key` secret reference at the *old* `appadino-discoveryAI-key` Key Vault entry — fixed via `az containerapp job secret set` to point at `appadino-discovery-2026-09` instead (same secret/env-var names, `ANTHROPIC_API_KEY`, just the underlying `keyVaultUrl` changed); (3) no revision-restart was needed or possible — Container Apps *Jobs* (unlike long-running Container Apps) have no persistent revision, and `keyvaultref` secrets resolve fresh from Key Vault at the start of each execution, so the very next job run picks up the new value automatically (confirmed no execution was running mid-update); (4) the old key was revoked in the Anthropic Console (confirmed by Duan, console-only, not independently verifiable via CLI); (5) live smoke-tested with a direct Messages API call using the new key's actual value (not the container's managed-identity path, which is architecturally guaranteed instead via the confirmed `Key Vault Secrets User` RBAC role at the vault scope + the corrected `keyVaultUrl`) — succeeded (`stop_reason: end_turn`, real token usage returned). The old `appadino-discoveryAI-key` secret still exists in the vault (unreferenced, holds a revoked key) — harmless to leave, fine to delete as cleanup whenever convenient.
- Gate E1 is now closed (GO, 2026-09-19, see "Done" above) — `pipeline/scripts/e1_fullenrich_spike.py`, `discovery.models.Enrichment`, and the `enrichments` table (migration `b7d4f1a9c3e6`) all now exist. E2 (see "In progress" above) is now built too — a real per-prospect Enrich button exists and is wired up — but it still only ever fires through hard rule 8's guard (`approved` + `enrichment_enabled` + `fullenrich_subaccount_id`), and `enrichment_enabled` is correctly `false` for the only tenant that exists, so nothing has actually auto-triggered or could have this session.
- **`dashboard/functions_api/local.settings.json` now holds a real dev `DATABASE_URL`** (Duan supplied it this session, gitignored, not committed) — future sessions can `func start` against real data directly, no placeholder-connection-string workaround needed. It does **not** yet have a `FULLENRICH_API_KEY` — add one to actually exercise a live Enrich click end-to-end (see E2's "not verified" note above).
- `discovery.clients.propublica.get_organization()` still exists and is still unused.
- The ARCHITECT client's id in the dev DB is **2**, not 1 — a downgrade/re-upgrade cycle during G1.3 consumed id 1. Harmless, but don't assume `client_id=1` when testing.
- Real data now populated for `client_id=2` in the dev DB: 154 `scores` rows (haiku+sonnet), 154 `prospects` rows, `qa_samples` rows from 4 separate debugging runs this gate (harmless — each run just inserts more sample rows, doesn't invalidate anything), 17 `suppression` seed rows. All real, not test fixtures; fine to leave in place or clear before a future real run.
- New Azure Monitor alert infrastructure this gate: `adisc-dev-ops-ag` action group emails **theoriginalcpututor@gmail.com** on any `RUN_FAILED`/`ZERO_OUTPUT_RUN`/`QA_MISMATCH_RATE_EXCEEDED` log marker, checked hourly. Small new recurring Azure cost (~$5-6/mo for the 3 scheduled query rules).
for multiuser launch remember:
Auth: migrate off personal-linked API key before multi-user launch
Current Discovery pipeline key is linked to an individual Console account (personal, workspace-scoped). Fine for single-operator use; not appropriate once this scales to multiple users/customers. Before go-live:

Create a service account (Settings → Service accounts) for the pipeline itself, scoped to its workspace — decouples the credential from any one person's identity.
Evaluate Workload Identity Federation (Settings → Workload identity) as a further step — replaces the static key with short-lived OIDC tokens, removing the long-lived secret from Key Vault entirely. Bigger lift (needs a trust source — Entra ID, GitHub Actions, etc.), worth it once there's more at stake than there is now.